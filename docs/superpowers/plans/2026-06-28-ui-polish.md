# UI 优化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 简化主题系统、整合设置入口、看板中文化及修复刷新 bug

**Architecture:** 纯前端改动，4 个文件修改。主题系统删除 system 模式；设置面板从 2 项扩展为分组面板（外观/功能/其他）；看板列标题中文化 + radix-vue Tooltip；Tab 切换持久化到 localStorage；KanbanView props 从 store.tasks 改为 store.pendingTasks 修复刷新 bug。

**Tech Stack:** Vue 3 + TypeScript + Tailwind CSS + radix-vue (Popover, Tooltip) + motion-v

---

## File Structure

| 文件 | 职责 | 改动类型 |
|------|------|---------|
| `client/src/composables/useTheme.ts` | 主题状态管理 | 修改：删除 system 模式 |
| `client/src/components/SettingsPanel.vue` | 设置面板 | 重写：分组面板 + 主题分段选择器 + 快捷键链接 |
| `client/src/views/KanbanView.vue` | 看板视图 | 修改：列中文化 + Tooltip + Popover 文案 |
| `client/src/App.vue` | 主应用 | 修改：header 按钮合并 + Tab 持久化 + KanbanView props 修复 + SettingsPanel props 调整 |

---

### Task 1: useTheme 简化 — 删除 system 模式

**Files:**
- Modify: `client/src/composables/useTheme.ts`

- [ ] **Step 1: 修改 Theme 类型和核心逻辑**

将 `useTheme.ts` 改为：

```ts
import { ref } from 'vue'

type Theme = 'light' | 'dark'

const theme = ref<Theme>('light')
const isDark = ref(false)
let initialized = false

function applyTheme() {
  isDark.value = theme.value === 'dark'
  document.documentElement.classList.toggle('dark', isDark.value)
  document.documentElement.classList.toggle('light', !isDark.value)
}

export function useTheme() {
  if (!initialized) {
    initialized = true
    const saved = localStorage.getItem('twm-theme') as Theme | null
    if (saved === 'light' || saved === 'dark') {
      theme.value = saved
    } else if (saved === 'system' || !saved) {
      const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches
      theme.value = prefersDark ? 'dark' : 'light'
    }
    applyTheme()
  }

  function setTheme(t: Theme) {
    theme.value = t
    localStorage.setItem('twm-theme', t)
    applyTheme()
  }

  function toggleTheme() {
    setTheme(theme.value === 'light' ? 'dark' : 'light')
  }

  return { theme, isDark, setTheme, toggleTheme }
}
```

关键变更：
- `Theme` 类型删除 `'system'`
- `applyTheme` 不再判断 system，直接 `theme.value === 'dark'`
- 删除 `getSystemTheme()` 函数
- 删除 `matchMedia` change 监听
- `toggleTheme` 简化为 `light ↔ dark`
- localStorage 读取时兼容旧值 `'system'`：fallback 到系统偏好

- [ ] **Step 2: 运行 typecheck 验证**

Run: `cd client && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: Commit**

```bash
git add client/src/composables/useTheme.ts
git commit -m "refactor: simplify useTheme — remove system mode, keep light/dark only"
```

---

### Task 2: SettingsPanel 重写 — 分组面板

**Files:**
- Rewrite: `client/src/components/SettingsPanel.vue`

- [ ] **Step 1: 重写 SettingsPanel.vue**

```vue
<script setup lang="ts">
import { Motion } from 'motion/vue'
import { Volume2, Medal, Keyboard, Sun, Moon } from 'lucide-vue-next'
import { ToggleSwitch } from './ToggleSwitch.vue'

const props = defineProps<{
  pos: { top: number; left: number }
  theme: 'light' | 'dark'
  soundEnabled: boolean
  achievementEnabled: boolean
}>()

const emit = defineEmits<{
  (e: 'update:theme', value: 'light' | 'dark'): void
  (e: 'update:soundEnabled', value: boolean): void
  (e: 'update:achievementEnabled', value: boolean): void
  (e: 'showHelp'): void
  (e: 'close'): void
}>()

const panelStyle = {
  background: 'var(--glass-overlay-bg)',
  backdropFilter: 'blur(var(--glass-overlay-blur))',
  WebkitBackdropFilter: 'blur(var(--glass-overlay-blur))',
  border: '1px solid var(--glass-overlay-border)',
  boxShadow: 'var(--shadow-dropdown), var(--glass-overlay-inset)',
}

const sectionLabelStyle = {
  color: 'var(--txt-muted)',
  fontSize: '10px',
  fontWeight: 600,
  textTransform: 'uppercase' as const,
  letterSpacing: '0.05em',
}

function handleClickOutside(e: MouseEvent) {
  if (!(e.target as HTMLElement).closest('[data-settings-panel]')) {
    emit('close')
  }
}
</script>

<template>
  <div data-settings-panel @click="handleClickOutside">
    <Motion
      :initial="{ opacity: 0, scale: 0.94, y: -8 }"
      :animate="{ opacity: 1, scale: 1, y: 0 }"
      :exit="{ opacity: 0, scale: 0.94, y: -8 }"
      :transition="{ type: 'spring', stiffness: 420, damping: 30 }"
      :style="{ ...panelStyle, position: 'fixed', top: pos.top + 'px', left: pos.left + 'px', width: 220 + 'px', zIndex: 9999, borderRadius: 16 + 'px', overflow: 'hidden', padding: '12px' }"
    >
      <!-- 标题 -->
      <div class="text-xs font-bold mb-3" :style="{ color: 'var(--txt-primary)' }">设置</div>

      <!-- 外观 -->
      <div class="mb-3">
        <div class="mb-1.5" :style="sectionLabelStyle">外观</div>
        <div class="flex gap-1">
          <button
            class="flex-1 flex items-center justify-center gap-1.5 py-1.5 rounded-xl text-[11px] font-semibold transition-colors cursor-pointer"
            :style="theme === 'light'
              ? { background: 'var(--accent-indigo)', color: 'var(--txt-on-color)' }
              : { background: 'var(--glass-input-bg)', color: 'var(--txt-muted)' }"
            @click="emit('update:theme', 'light')"
          ><Sun :size="12" />浅色</button>
          <button
            class="flex-1 flex items-center justify-center gap-1.5 py-1.5 rounded-xl text-[11px] font-semibold transition-colors cursor-pointer"
            :style="theme === 'dark'
              ? { background: 'var(--accent-indigo)', color: 'var(--txt-on-color)' }
              : { background: 'var(--glass-input-bg)', color: 'var(--txt-muted)' }"
            @click="emit('update:theme', 'dark')"
          ><Moon :size="12" />深色</button>
        </div>
      </div>

      <!-- 功能 -->
      <div class="mb-3">
        <div class="mb-1.5" :style="sectionLabelStyle">功能</div>
        <div class="flex items-center justify-between py-1.5">
          <div class="flex items-center gap-2 text-[12px]" :style="{ color: 'var(--txt-primary)' }">
            <Volume2 :size="13" />音效
          </div>
          <ToggleSwitch :model-value="soundEnabled" @update:model-value="emit('update:soundEnabled', $event)" />
        </div>
        <div class="flex items-center justify-between py-1.5">
          <div class="flex items-center gap-2 text-[12px]" :style="{ color: 'var(--txt-primary)' }">
            <Medal :size="13" />完成成就
          </div>
          <ToggleSwitch :model-value="achievementEnabled" @update:model-value="emit('update:achievementEnabled', $event)" />
        </div>
      </div>

      <!-- 其他 -->
      <div>
        <div class="mb-1.5" :style="sectionLabelStyle">其他</div>
        <button
          class="w-full flex items-center justify-between py-1.5 text-[12px] transition-colors cursor-pointer"
          :style="{ color: 'var(--txt-primary)' }"
          @click="emit('showHelp')"
        >
          <div class="flex items-center gap-2">
            <Keyboard :size="13" />快捷键
          </div>
          <span class="text-[10px]" :style="{ color: 'var(--accent-indigo)' }">查看 →</span>
        </button>
      </div>
    </Motion>
  </div>
</template>
```

- [ ] **Step 2: 运行 typecheck 验证**

Run: `cd client && npx tsc --noEmit`
Expected: 可能有 App.vue 类型错误（因为 SettingsPanel props 变了），Task 3 会修复

- [ ] **Step 3: Commit**

```bash
git add client/src/components/SettingsPanel.vue
git commit -m "refactor: rewrite SettingsPanel with grouped layout and theme segment selector"
```

---

### Task 3: App.vue — header 按钮合并 + SettingsPanel 适配 + Tab 持久化 + KanbanView props 修复

**Files:**
- Modify: `client/src/App.vue`

- [ ] **Step 1: 修改 header 按钮区域**

找到 App.vue 中左面板 header 的三个按钮区域（快捷键、设置、主题循环），替换为一个齿轮图标按钮。

删除以下三个按钮：
- 快捷键按钮（`@click="showHelp = true"`，Keyboard 图标）
- 设置按钮（`@click="openSettings"`，Settings 图标）
- 主题按钮（`@click="toggleTheme"`，Sun/Moon/Monitor 图标）

替换为一个按钮：
```html
<button
  ref="settingsBtnRef"
  class="p-2 rounded-xl transition-colors cursor-pointer"
  :class="ctrlBtnClass()"
  @click="openSettings"
>
  <Settings :size="14" />
</button>
```

- [ ] **Step 2: 删除 themeIcon 计算属性**

删除 App.vue 中的 `themeIcon` 计算属性（不再需要，主题切换已移入 SettingsPanel）。

- [ ] **Step 3: 修改 SettingsPanel 调用**

将 App.vue 中 SettingsPanel 的调用从：
```html
<SettingsPanel
  v-if="showSettings"
  :pos="settingsPos"
  :sound-enabled="soundEnabled"
  :achievement-enabled="achievementEnabled"
  @update:sound-enabled="..."
  @update:achievement-enabled="..."
  @close="showSettings = false"
/>
```

改为：
```html
<SettingsPanel
  v-if="showSettings"
  :pos="settingsPos"
  :theme="theme"
  :sound-enabled="soundEnabled"
  :achievement-enabled="achievementEnabled"
  @update:theme="setTheme"
  @update:sound-enabled="soundEnabled = $event"
  @update:achievement-enabled="achievementEnabled = $event; try { localStorage.setItem('twm-achievement', String($event)) } catch {}"
  @show-help="showHelp = true; showSettings = false"
  @close="showSettings = false"
/>
```

- [ ] **Step 4: Tab 持久化**

修改 `currentView` 初始化：
```ts
const currentView = ref<ViewType>((localStorage.getItem('twm-view') as ViewType) || 'tasks')
```

在 `watch(currentView, ...)` 回调末尾添加：
```ts
localStorage.setItem('twm-view', view)
```

- [ ] **Step 5: KanbanView props 修复**

将 KanbanView 的 `:tasks` 从 `store.tasks` 改为 `store.pendingTasks`：
```html
<KanbanView
  :tasks="store.pendingTasks"
  ...
```

- [ ] **Step 6: 清理未使用的 import**

删除 App.vue 中不再使用的 import：
- `Monitor`（来自 lucide-vue-next，system 模式图标）
- `Keyboard`（快捷键按钮已移除，但 HelpModal 仍需保留其 import 如果其他地方用到）

检查 `Sun`、`Moon` 是否仍被其他地方使用，若不再使用也删除。

- [ ] **Step 7: 运行 typecheck 验证**

Run: `cd client && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 8: Commit**

```bash
git add client/src/App.vue
git commit -m "fix: merge header buttons into settings, persist tab, fix kanban refresh bug"
```

---

### Task 4: KanbanView — 列中文化 + Tooltip + Popover 文案

**Files:**
- Modify: `client/src/views/KanbanView.vue`

- [ ] **Step 1: 添加 radix-vue Tooltip import**

在 KanbanView.vue 的 `<script setup>` 中添加：
```ts
import { TooltipRoot, TooltipTrigger, TooltipPortal, TooltipContent, TooltipProvider } from 'radix-vue'
```

- [ ] **Step 2: 修改 cols 定义**

将 cols 定义改为：
```ts
const cols = [
  { key: 'inbox', label: '还在看', tip: '新建的任务默认在这里，还没分配项目', filter: (t: Task) => t.status !== 'completed' && t.status === 'pending' && !t.project },
  { key: 'backlog', label: '排队中', tip: '编辑任务归属项目后自动移入', filter: (t: Task) => t.status !== 'completed' && t.status === 'pending' && !!t.project },
  { key: 'ip', label: '做着呢', tip: '点击"开始计时"后移入，表示正在做', filter: (t: Task) => t.status !== 'completed' && t.status === 'started' },
  { key: 'hold', label: '等一下', tip: '手动设置为搁置状态的任务', filter: (t: Task) => t.status !== 'completed' && t.status === 'on-hold' },
  { key: 'done', label: '搞定了', tip: '已完成的任务', filter: (t: Task) => t.status === 'completed' },
]
```

- [ ] **Step 3: 修改列标题模板，添加 Tooltip**

将列标题区域从：
```html
<div class="flex items-center gap-2 px-3 py-3 shrink-0">
  <span class="text-xs font-bold" :style="{ color: 'var(--txt-primary)' }">{{ col.label }}</span>
  <span class="text-[10px] px-1.5 py-0.5 rounded-full font-semibold"
    :style="{ background: 'var(--glass-input-bg)', color: 'var(--txt-muted)' }"
  >{{ col.tasks.length }}</span>
</div>
```

改为：
```html
<div class="flex items-center gap-2 px-3 py-3 shrink-0">
  <TooltipProvider :delay-duration="300">
    <TooltipRoot>
      <TooltipTrigger asChild>
        <span class="text-xs font-bold cursor-default" :style="{ color: 'var(--txt-primary)' }">{{ col.label }}</span>
      </TooltipTrigger>
      <TooltipPortal>
        <TooltipContent
          :side-offset="6"
          class="z-50 px-3 py-1.5 rounded-xl text-[11px] max-w-[200px]"
          :style="{
            background: 'var(--glass-modal-bg)',
            backdropFilter: 'blur(var(--glass-modal-blur))',
            WebkitBackdropFilter: 'blur(var(--glass-modal-blur))',
            border: '1px solid var(--glass-modal-border)',
            color: 'var(--txt-primary)',
            boxShadow: 'var(--shadow-dropdown)',
          }"
        >{{ col.tip }}</TooltipContent>
    </TooltipPortal>
    </TooltipRoot>
  </TooltipProvider>
  <span class="text-[10px] px-1.5 py-0.5 rounded-full font-semibold"
    :style="{ background: 'var(--glass-input-bg)', color: 'var(--txt-muted)' }"
  >{{ col.tasks.length }}</span>
</div>
```

- [ ] **Step 4: 修改 Popover 菜单文案**

将 Popover 中的：
- `"移动到 Inbox"` → `"移动到还在看"`
- `"移动到 Backlog"` → `"移动到排队中"`

- [ ] **Step 5: 运行 typecheck 验证**

Run: `cd client && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 6: Commit**

```bash
git add client/src/views/KanbanView.vue
git commit -m "feat: kanban column Chinese labels + tooltips + popover i18n"
```

---

### Task 5: 全局验证

**Files:** 无新改动

- [ ] **Step 1: 前端 typecheck**

Run: `cd client && npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 2: 前端构建**

Run: `cd client && npx vite build`
Expected: 构建成功

- [ ] **Step 3: 后端检查**

Run: `cd server && cargo check`
Expected: 无错误

- [ ] **Step 4: 后端测试**

Run: `cd server && cargo test`
Expected: 全部通过

- [ ] **Step 5: 手动功能验证清单**

在浏览器中验证以下功能：
1. [ ] 设置面板：点击齿轮图标打开，外观分段选择器切换 dark/light
2. [ ] 设置面板：音效/成就 Toggle 正常
3. [ ] 设置面板：点击"快捷键 →"打开 HelpModal
4. [ ] Tab 切换后刷新页面，保持选中状态
5. [ ] 看板列标题显示中文名
6. [ ] 看板列标题 hover 显示 Tooltip
7. [ ] 看板 Popover 菜单显示中文"移动到还在看/排队中"
8. [ ] 看板列间移动任务后页面自动刷新
